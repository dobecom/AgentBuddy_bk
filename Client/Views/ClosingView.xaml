<UserControl x:Class="Client.Views.ClosingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<UserControl.DataContext>
        <vm:ClosingViewModel />
    </UserControl.DataContext>-->
    <StackPanel>
        <TextBlock Text="Case Summary" FontWeight="Bold" />
        <TextBox Text="{Binding CaseSummary}" IsReadOnly="True" AcceptsReturn="True" Height="80" />
        <TextBox Text="{Binding ClosingEmailDraft, Mode=TwoWay}" AcceptsReturn="True" Height="80" />
        <Button Content="Send Closing Email" Command="{Binding SendClosingEmailCommand}" />
        <Button Content="Sync to OneNote" Command="{Binding SyncOneNoteCommand}" />
    </StackPanel>
</UserControl>
