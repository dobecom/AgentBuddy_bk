<UserControl x:Class="Client.Views.ScopingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Client.ViewModels"
             xmlns:local="clr-namespace:Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<UserControl.DataContext>
        <vm:ScopingViewModel /> 
    </UserControl.DataContext>-->
    <StackPanel>
        <TextBlock Text="Case Overview" FontWeight="Bold" />
        <!-- Bind to CurrentCase properties -->
        <TextBlock Text="{Binding SuggestedCaseType}" />
        <Button Content="Categorize Case" Command="{Binding CategorizeCaseCommand}" />
        <TextBox Text="{Binding FollowUpDraft, Mode=TwoWay}" AcceptsReturn="True" Height="80" />
        <Button Content="Send Follow-up Email" Command="{Binding SendFollowUpCommand}" />
        <Button Content="Save Case" Command="{Binding SaveCaseCommand}" /> <!-- New Button Added -->
    </StackPanel>
</UserControl>
