<UserControl x:Class="Client.Views.AnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Client.ViewModels"
             xmlns:local="clr-namespace:Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<UserControl.DataContext>
        <vm:AnalysisViewModel />
    </UserControl.DataContext>-->
    <StackPanel>
        <Button Content="Run AI Analysis" Command="{Binding RunAnalysisCommand}" />
        <TextBox Text="{Binding AnalysisResult}" IsReadOnly="True" AcceptsReturn="True" Height="120" />
        <Button Content="Draft Customer Response" Command="{Binding DraftResponseCommand}" />
    </StackPanel>
</UserControl>
