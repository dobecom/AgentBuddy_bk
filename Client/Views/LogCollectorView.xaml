<UserControl x:Class="Client.Views.LogCollectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Client.ViewModels"
             xmlns:local="clr-namespace:Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<UserControl.DataContext>
        <vm:LogCollectorViewModel />
    </UserControl.DataContext>-->
    <StackPanel>
        <Button Content="Add Log Files" Command="{Binding AddFilesCommand}" />
        <DataGrid ItemsSource="{Binding LogFiles}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" />
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" />
            </DataGrid.Columns>
        </DataGrid>
        <!--<Button Content="Upload Files" Command="{Binding UploadFilesCommand}" IsEnabled="{Binding IsUploading, Converter={StaticResource InverseBoolConverter}}" />-->
        <!--<ProgressBar IsIndeterminate="{Binding IsUploading}" Visibility="{Binding IsUploading, Converter={StaticResource BoolToVisibilityConverter}}" />-->
    </StackPanel>
</UserControl>
